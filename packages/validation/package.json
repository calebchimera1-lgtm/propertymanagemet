{
  "name": "@pm/validation",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@pm/types": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@pm/config": "workspace:*",
    "typescript": "^5.7.3"
  }
}
